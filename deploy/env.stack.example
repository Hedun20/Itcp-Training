# Confirm the exact DNS spelling before deployment.
APP_DOMAIN=icp-europe.nl

# The application binds only to localhost. The hosting panel or host Nginx
# must proxy the public HTTPS domain to this port.
APP_HTTP_PORT=8088
